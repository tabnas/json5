{
  "name": "@tabnas/json5",
  "version": "0.5.5",
  "description": "This plugin configures the [Tabnas](https://github.com/tabnas/json5) JSON parser to parse JSON5 syntax.",
  "main": "dist/json5.js",
  "type": "commonjs",
  "browser": "json5.min.js",
  "types": "dist/json5.d.ts",
  "homepage": "https://github.com/tabnas/json5",
  "keywords": [
    "json5",
    "json",
    "parser",
    "tabnas"
  ],
  "author": "Richard Rodger (http://richardrodger.com)",
  "repository": {
    "type": "git",
    "url": "git://github.com/tabnas/json5.git",
    "directory": "ts"
  },
  "scripts": {
    "gen-suite-expected": "node ../scripts/gen-json5-expected.js",
    "check-suite-expected": "node ../scripts/gen-json5-expected.js --check",
    "pretest": "npm run check-suite-expected",
    "test": "node --enable-source-maps --test \"dist-test/*.test.js\"",
    "test-some": "node --enable-source-maps --test-name-pattern=\"$npm_config_pattern\" --test \"dist-test/*.test.js\"",
    "test-watch": "node --test --watch dist-test/json5.test.js",
    "embed": "node embed-grammar.js",
    "watch": "tsc --build src test -w",
    "build": "node embed-grammar.js && tsc --build src test",
    "clean": "rm -rf node_modules dist dist-test yarn.lock package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: ts/v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag ts/v$REPO_VERSION && git push origin ts/v$REPO_VERSION;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run repo-tag",
    "repo-stage": "npm run clean && npm i && npm run repo-stage-quick",
    "repo-stage-quick": "npm run build && npm run test && npm stage publish",
    "repo-stage-list": "npm stage list",
    "repo-stage-approve": "npm stage approve"
  },
  "license": "MIT",
  "files": [
    "src",
    "dist",
    "LICENSE"
  ],
  "devDependencies": {
    "@tabnas/debug": "*",
    "@tabnas/jsonic": "*",
    "@tabnas/parser": "*",
    "@tabnas/railroad": "*",
    "@tabnas/support": "*",
    "@types/node": "^25.6.0",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "@tabnas/jsonic": ">=0",
    "@tabnas/parser": ">=0"
  },
  "engines": {
    "node": ">=24"
  }
}
